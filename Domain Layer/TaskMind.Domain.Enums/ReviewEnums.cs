namespace TaskMind.Domain.Enums
{
    /// <summary>Đối tượng được đánh giá: người dùng, công ty hay cơ sở đào tạo (mục 4.19).</summary>
    public enum ReviewTargetType
    {
        User,
        Company,
        School
    }
}
